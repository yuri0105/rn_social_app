import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        margin: 10
    },
    text: {
        marginLeft: 10,
        fontSize: 17,
        fontWeight: 'bold'
    },
    textInput: { 
        height: 40, 
        borderColor: 'gray', 
        margin: 10,
        backgroundColor: "lightgray",
        borderRadius: 10,
        flexDirection:"row",
        alignItems: "center"
    
    }
})